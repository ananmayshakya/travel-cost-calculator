DATABASE_URL=
PORT=4000

# origin allowed to call this API in dev (the Vite frontend)
FRONTEND_ORIGIN=http://localhost:5173

# fuel price provider: "mock" (default, plausible fake prices for dev) or "live" (real source, needs a key below)
FUEL_SOURCE=mock
FUEL_PRICE_API_KEY=
